apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: covidomysqlpvc
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  storageClassName: covidostorageclass
